# Assets

Place reusable templates or static assets here only when they are used by generated outputs.

Avoid storing large source archives in this directory. AFSIM source code should remain in its own source root and be referenced by path.
